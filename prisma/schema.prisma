generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model bunditcmru {
  id       BigInt  @id @default(autoincrement())
  c        BigInt?
  counting BigInt?
  konrub   BigInt?
  timeuse  String?
  un       String?
  h        BigInt?
  m        BigInt?
  s        BigInt?
}
