// id: data?.id.toString(),
// c: data?.c?.toString(),
// counting: data?.counting?.toString(),
// konrub: data?.konrub?.toString(),
// timeuse: data?.timeuse,
// un: data?.un,
// to interface

interface ApiData {
  id: string;
  c: number;
  counting: number;
  konrub: number;
  timeuse: string;
  un: string;
}
